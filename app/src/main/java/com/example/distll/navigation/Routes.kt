package com.example.distll.navigation

/** Single source of truth for nav route strings - referenced by NavHost and by NavigationContents. */
object Routes {
    const val HOME = "home"
    const val ANALYSIS = "analysis"
    const val SETTINGS = "settings"
    const val PROFILE = "profile"
}
